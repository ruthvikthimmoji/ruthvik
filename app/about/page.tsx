import AboutClient from "./AboutClient";


export const metadata = {
  title: "About | Ruthvik — UI/UX Designer",
  description: "A designer’s journey, values, and approach — told like a book.",
};

export default function AboutPage() {
  return <AboutClient />;
}
